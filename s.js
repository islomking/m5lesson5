let ot = prompt("ismingizni kiriting")

let space = prompt("space di idni kiriting")

let password = prompt("space dagi passwordingiz")

let all = {
    name: `${ot}`,
    id: `${space}`,
    password: `${password}`
};

console.log(all);